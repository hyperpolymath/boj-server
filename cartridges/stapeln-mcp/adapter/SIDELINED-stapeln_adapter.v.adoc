= SIDELINED: stapeln_adapter.v
:toc:
:icons: font

== Why this file was removed

V-lang was banned estate-wide on 2026-04-12.
The V-lang adapter `stapeln_adapter.v` has been replaced by the Zig
three-protocol adapter `stapeln_adapter.zig`.

== Replacement

`stapeln_adapter.zig` provides identical tool dispatch across three protocols:

* REST (port 9112)
* gRPC-compat (port 9113)
* GraphQL (port 9114)

The FFI contract is unchanged — `stapeln_ffi.zig` is unmodified.

== Migration notes

* V-lang detected via `v.mod`/`vpkg.json`, NOT `.v` extension (Verilog collision).
* Original `.v` adapter preserved in git history.
* All tool names and semantics faithfully preserved.

== See also

* `stapeln_adapter.zig` — replacement adapter
* `build.zig` — build configuration
* `../ffi/stapeln_ffi.zig` — Zig FFI (unchanged)
* `../cartridge.json` — tool schema (newly created)
* `../mod.js` — Deno handler (newly created)
