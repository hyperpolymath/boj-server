// SPDX-License-Identifier: PMPL-1.0-or-later
= Bundle of Joy Server — Roadmap
:toc:

== Vision

A distributed, formally verified catalogue of developer server capabilities. AI goes to ONE place instead of hunting across dozens of MCP/LSP/etc servers. Community-hosted nodes share the load.

== Current Status: v1.0.1 Recovery (Grade D Alpha)

Core Elixir/BEAM REST layer is live: 112 cartridges auto-discovered at startup,
JS dispatch via persistent Deno worker pool, Zig FFI path for `.so` cartridges,
X25519/ChaCha20-Poly1305 Option A credential forwarding, 50 ExUnit tests passing.
V-lang adapter migration complete (0 `.v` files). All 112 cartridges have
`cartridge.json` metadata + `mod.js` tool handlers.  Grade C dogfooding requires
expanding to 165+ tests (see `TEST-NEEDS.md`).

== Milestones

=== v0.1.0 — Foundation (Phase 0+1) ✓

* [x] Fresh repo from rsr-template-repo (17 mandatory workflows)
* [x] Idris2 ABI: Catalogue, Protocol, Domain, Menu, Federation
* [x] `IsUnbreakable` proof type (only Ready cartridges can mount)
* [x] Zig FFI: catalogue mount/unmount with safety gate
* [x] C header: `boj_catalogue.h` from Idris2 encodings
* [x] Teranga menu (`menu.a2ml`) with three tiers
* [x] Order-ticket protocol (`order-ticket.scm`)
* [x] Three cartridges: database-mcp, nesy-mcp, fleet-mcp
* [x] Cultural respect documentation (Teranga/Umoja/Ayo)
* [x] Federation design (Umoja gossip, hash attestation)

=== v0.2.0 — Live Cartridges (Phase 2-3)

* [x] Zig FFI: dynamic cartridge loader with hash verification
* [x] V-lang triple adapter: REST (9000) + gRPC (9001) + GraphQL (9002)
* [x] First working MCP endpoint (database-mcp)
* [x] First working LSP endpoint (database-mcp)
* [x] Order-ticket flow end-to-end (read menu → place order → get endpoints)
* [x] Stapeln container image (Chainguard base, Podman)

=== v0.3.0 — Umoja Network (Phase 5)

* [x] Gossip protocol implementation (IPv6, node discovery)
* [x] Hash attestation (binary verification for community nodes)
* [x] Seed node setup (4 family nodes across 4 continents)
* [ ] Load-aware routing (requests to healthiest node)
* [x] Auto-SDP (Software Defined Perimeter for nodes)
* [ ] DoQ/DoH for encrypted DNS resolution

=== v0.4.0 — Community (Phase 5+)

* [x] PanLL panel (BoJ matrix display, menu browser, order placement)
* [ ] IndieWeb integration (Webmention, self-hosted node discovery)
* [x] Community cartridge submission process (Ayo menu)
* [x] Wiki with guides for node operators
* [ ] Project board for cartridge development tracking

=== v0.5.0 — Shield (Security)

* [x] SDP cartridge (auto-SDP for community nodes)
* [ ] DNS Shield cartridge (DoQ/DoH/oDNS)
* [ ] Container hash state monitoring (vordr integration)
* [ ] PMPL provenance chain verification

=== v1.0.0 — Matrix Fill (Phase 6)

* [x] Remaining MCP cartridges (Cloud, Container, K8s, Git, Secrets, Queues, IaC, Observe, SSG, Proof)
* [x] LSP cartridges for key domains
* [x] DAP cartridges where relevant
* [x] polystack deprecation (Phase 7)

=== v1.0.1 — Adapter Migration Recovery (P0, In Progress)

* [x] Remove remaining V-lang adapters across `cartridges/` (confirmed 0 `.v` files 2026-04-25 — cleanup completed 2026-04-12)
* [x] Ensure every cartridge has `cartridge.json` metadata (all 112 cartridges covered 2026-04-25)
* [x] Ensure every cartridge has `mod.js` tool handlers (all 112 cartridges covered 2026-04-25)
* [ ] Re-run cartridge matrix build/tests and update `TEST-NEEDS.md` with current evidence
* [ ] Refresh repo documentation claims after migration closure (counts, completion text, handover docs)

=== v1.1.0 — JS Dispatch + Gateway Sidecar

JS cartridge dispatch (the large remaining gap — 99 of 106 cartridges use `mod.js`, not Zig `.so`):

* [x] `BojRest.JsInvoker` — shells out to `deno run mod.js` per invocation (Phase 1: fork-per-call, ~200 ms cold-start)
* [x] Router dispatch branch: if `cart["ffi"]` key present → `Invoker` (Zig path); else → `JsWorkerPool` (Deno pool path)
* [x] Fix `cartridge_so_path/1` to read `cart["ffi"]["so_path"]` from the manifest instead of hardcoding the derivation
* [x] `BojRest.JsWorkerPool` + `BojRest.JsWorker` — persistent Deno worker pool (Phase 2); consistent hash on mod path for module-cache locality; `JsInvoker` fork-per-call as fallback when pool absent
* [ ] End-to-end test: `curl POST /cartridge/boj-health/invoke` (Zig) + `curl POST /cartridge/local-memory-mcp/invoke` (JS)

Gateway sidecar (`http-capability-gateway` in front of boj-server):

* [x] Add `gateway-policy.yaml` (done — `container/gateway-policy.yaml`) to dev compose as optional sidecar
* [x] Implement `PolicyLoader.load_from_boj_catalog/1` in `http-capability-gateway` — reads `BOJ_CARTRIDGES_ROOT/*/cartridge.json` at boot, infers per-cartridge invoke exposure from `auth.method` (`none` → `public`, others → `authenticated`). Zero manual policy maintenance thereafter. See `docs/gateway-catalog-integration.adoc`.
* [ ] Update `compose.dev.yaml` with gateway sidecar entry (port 7800 → boj-rest:7700)
* [ ] Retire static `gateway-policy.yaml` once catalog mode is live

== Future Directions

* **oDNS relay**: Distributed Oblivious DNS as a network service (research problem)
* **Aerie integration**: Dynamic threat response via BoJ cartridges
* **Cross-network federation**: BoJ nodes discovering other federated networks
* **Mobile nodes**: Tauri/Dioxus-based node management app

== How to Contribute

* **Propose a cartridge**: Open a Discussion using the "Cartridge Proposal" template
* **Host a node**: Open a Discussion using the "Hosting" template
* **Build a cartridge**: See `docs/DEVELOPERS.md`
* **Give feedback**: Open a Discussion or Issue
