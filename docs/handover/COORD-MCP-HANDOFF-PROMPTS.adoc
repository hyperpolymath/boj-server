== Handoff prompts — coord-mcp + 007-mcp work

Ready-to-paste prompts for continuing the work started in session
2026-04-20. Copy the one that matches your next step.

Source of truth: `+Desktop/COORD-MCP-DESIGN-LOG.md+` (end-to-end design
doc, 30 design decisions, 12 appendices).

=== Status snapshot — 2026-04-20 afternoon

* *Prompt 1 lane (Task #7 durability)* — ✅ shipped end 2026-04-20
morning.
* *Prompt 3 lane (coord finishers #13–17 + #32/35/36/37)* — ✅ shipped
2026-04-20 afternoon. All 11 tasks in this lane complete and pushed. See
`+COORD-MCP-DESIGN-LOG.md+` Part 2 for the full ledger.
* *Prompt 2 lane (007-mcp family #9–12)* — ⏳ in progress in a parallel
session. Touches `+007-lang/+` and `+cartridges/007-mcp/+`; no overlap
with Prompt 3’s changes.
* *Remaining multi-model extension (#33, #34)* — deferred. See Appendix
M in the design log for specs.

'''''

=== Prompt 1 — Next session: continue Task #7 (VeriSimDB sidecar)

Use this in a *new Claude Code session* (fresh context). Best for
sequential continuation.

....
We're continuing work on the local-coord-mcp cartridge in the BoJ server.
The previous session shipped tasks 1, 3, 4, 5, 6, 16 and pushed them.

Before doing anything else:

1. Read Desktop/COORD-MCP-DESIGN-LOG.md end-to-end. It's 500 lines and
   contains every design decision (DD-1..DD-30), the task list, commit
   log, architectural rationale, and a handoff brief in Appendix L.

2. Read these memory files:
   - ~/.claude/projects/-var-mnt-eclipse-repos/memory/project_coord_supervision_architecture.md
   - ~/.claude/projects/-var-mnt-eclipse-repos/memory/project_federation_authoritative_site.md

3. cd /var/mnt/eclipse/repos/boj-server && git log --oneline -7

4. Read cartridges/local-coord-mcp/docs/envelope-design.adoc for the
   envelope rationale, and cartridges/local-coord-mcp/schemas/coord-
   messages-contracts.ncl for the Nickel contracts.

Your next task is #7 (VeriSimDB sidecar). Pass B per Appendix L:

- Tables: coord_inbox, coord_claims, coord_audit,
  coord_quarantine_overflow, coord_track_record
- Persist-on-write on every mutation (DD-20)
- Restart/replay: on adapter start, restore peer table + claim table +
  inbox ring buffers from durable state
- VeriSimDB lives at cartridges/verisimdb-mcp/ — use its FFI
- Per-box instance for coord (DD-23) — suggest /var/lib/boj-server/
  coord.verisim/

Do not:
- Redesign DD-1..DD-30 without flagging to the user first
- Implement choreographic/epistemic/echo/tropical types (Phase 2+/3)
- Touch v2 federation fields (Phase 4 — hermetic local for now)

Style rules:
- Commit ASAP, specific paths in git add
- Push origin after each commit
- Update Desktop/COORD-MCP-DESIGN-LOG.md at each checkpoint
- Ask before writing new memory files; explicit requests bypass

Start by reading the design log, confirming your understanding in one
paragraph, and asking me any blockers before you start implementing.
....

'''''

=== Prompt 2 — Parallel session: 007-mcp cartridge family (#9-12)

Use this in a *separate parallel session*, AFTER tasks #7 + #8 land.
This work is completely independent of coord layer — different
cartridge, different repo, different skill set.

....
We're building the 007-mcp BoJ cartridge for the 007-lang repo.
Context: 007 is a canonical-proof-suite + methodology repo with Coq/
Idris2 proofs. The cartridge exposes 007's CLI + methodology as MCP
tools so any AI session entering 007-lang automatically gets
appropriate context without recalling.

Before doing anything else:

1. Read Desktop/COORD-MCP-DESIGN-LOG.md — specifically DD-24, DD-25,
   DD-26 (cartridge location + CLI surface + memory auto-lift design).
2. Read ~/.claude/projects/-var-mnt-eclipse-repos/memory/project_coord_
   supervision_architecture.md to understand the interaction model.
3. cd /var/mnt/eclipse/repos/007-lang && cat 0-AI-MANIFEST.a2ml
4. Check what the oo7 CLI does:
   cd /var/mnt/eclipse/repos/007-lang && just --list | head -40

Your tasks in order:

#9: Create 007-mcp BoJ cartridge source
  - Location: inside 007-lang repo (DD-24), not in boj-server
  - Layout: abi/ ffi/ adapter/ cartridge.ncl schemas/
  - Expose the FULL oo7 CLI surface as MCP tools (DD-25)
  - Install hook in 007's Justfile deploys to boj-server/cartridges/
  - Domain=dezig, protocols=[MCP, Agentic]

#10: 007 on-enter/on-exit contractile hooks
  - On-enter: register coord peer, read STATE.a2ml + methodology.a2ml,
    announce status, return methodology pack to caller
  - On-exit: update STATE.a2ml, drift check, release claims, deregister

#11: Fill 007 missing bust + adjust contractiles
  - Bustfile.a2ml — what breaks (banned patterns, proof regression,
    spec drift — actions)
  - Adjustfile.a2ml — allowed drift corrections with runners
  - Wire into .machine_readable/contractiles/

#12: Memory auto-lift on 007 context entry
  - Dynamic tag-indexed lookup from VeriSimDB (DD-26)
  - Memories tagged; cartridge queries on 007 entry with repo-derived
    tags; returns matches as tool output

Do not:
- Touch cartridges/local-coord-mcp/ or mcp-bridge/ (coord layer is in
  active build-out in the sequential session; stay out of its way)
- Put SCM files in the root (estate rule: .machine_readable/ only)
- Break 007's existing Idris2 proofs

Style rules:
- Commit ASAP, one unit = one commit, specific paths in git add
- Push origin after each commit (GitHub is source of truth)
- Update Desktop/COORD-MCP-DESIGN-LOG.md at each checkpoint, specifically
  the commit log and relevant task rows

Start by reading the design log references, confirming understanding,
and asking any blockers before you start.
....

'''''

=== Prompt 3 — Parallel session: coord finishers (#13-15 + #17) — ✅ SHIPPED 2026-04-20 afternoon

All four core tasks plus the follow-on extension tasks (#32/35/36/37)
landed in one parallel session. Commits: `+f8cafbf+`, `+3bd4710+`,
`+6065878+`, `+9e40a86+`, `+ed85ca2+`, `+634c163+`, `+7f2f4a9+`,
`+aeae440+`, `+eba7cfe+` — all pushed to `+origin/main+`. See the design
log Part 2 ledger for the full mapping. Do NOT re-run this prompt.

The original prompt body is preserved below for archival purposes.

Use this in a *separate parallel session*, AFTER tasks #7 + #8 land.
Runs alongside Prompt 2 — they don’t touch the same code.

....
We're finishing the local-coord-mcp learning + validation layers.
Context: coord layer backbone (supervision, quarantine, envelope schema,
Nickel contracts) is in place. Now adding the adaptive-behaviour +
runtime-validation layers.

Before doing anything else:

1. Read Desktop/COORD-MCP-DESIGN-LOG.md — specifically DD-27, DD-28,
   DD-30 (dispatch preference + learning + attester selection).
2. Read ~/.claude/projects/-var-mnt-eclipse-repos/memory/project_coord_
   supervision_architecture.md.
3. cd /var/mnt/eclipse/repos/boj-server && git log --oneline -10
4. Read cartridges/local-coord-mcp/ffi/local_coord_ffi.zig and schemas/
   coord-messages-contracts.ncl to understand the integration points.

Your tasks in order:

#13: Track-record table in VeriSimDB + effective_affinity
  - Rows: (client_kind, task_tag, outcome, duration, risk_tier, timestamp)
    — note keys on client_kind not peer_id (track record survives peer
    restart, DD-29)
  - Window: last 20 attempts OR last 7 days, whichever is larger
  - New tool coord_get_affinities returns scores per (client_kind, tag)

#14: Reassignment suggestion engine + Opus review flow
  - Periodic scan over track-record, flags outliers:
    - confidence > 0.8 AND effective_affinity < 0.3 = flag overclaim
    - effective_affinity high but not in declared affinities = suggest promotion
    - effective_affinity low across N attempts = suggest removal
  - Surface to Opus as fyi/clarify envelopes, never auto-modify
  - Always loops back through supervisor + user before applying

#15: Dispatch-preference mode on claims
  - Extend claim envelope with dispatch_preference: deliberate/broadcast/auto
  - Add task_difficulty: trivial/routine/challenging/novel
  - Default: broadcast for trivial+routine, deliberate for challenging+novel
  - Rate-limit on rejections: 5 rejects / 10 min = 30s cooldown (confirmed)

#17: Deno Nickel shim in mcp-bridge — runtime envelope validation
  - Shell out to `nickel eval` with the envelope to apply contracts.validate
    from cartridges/local-coord-mcp/schemas/coord-messages-contracts.ncl
  - Cache contracts at bridge startup
  - Runs BEFORE dispatchLocalCoord forwards to Zig adapter
  - On failure: return MCP error with descriptive message

Do not:
- Touch 007-lang or cartridges/007-mcp/ (007-mcp parallel session works there)
- Auto-modify affinities without Opus + user review (never automate)
- Implement choreographic/epistemic/echo/tropical types (Phase 2+/3)

Style rules:
- Commit ASAP, one unit = one commit, specific paths in git add
- Push origin after each commit
- Update Desktop/COORD-MCP-DESIGN-LOG.md at each checkpoint

Start by reading the design log, confirming understanding in one
paragraph, and asking any blockers before you start.
....

'''''

=== Prompt 4 — Minimal single-line pickup (if design log is enough)

If you want the lightest possible handoff and the next session knows the
pattern:

....
Read Desktop/COORD-MCP-DESIGN-LOG.md Appendix L for the handoff brief.
Next task is #7 VeriSimDB sidecar Pass B. Start by reading the design
log end-to-end, then confirm understanding and ask any blockers.
....

'''''

=== Which prompt to use when

* *Right now (2026-04-20 afternoon onward)*: Only Prompt 2 (007-mcp
family) has active work remaining. Prompts 1 + 3 are complete.
* *After Prompt 2 ships*: pick up deferred items from Appendix M of the
design log — Tasks #33 (client_kind + variant) and #34 (capability
advertisement) are the natural next slice.
* *You trust the next Claude to self-orient*: Prompt 4.

=== Notes for using these

* All prompts tell the Claude to read the design log FIRST, confirm
understanding, and ask blockers before implementing. This catches drift
before code is written.
* The design log + memory files are the authoritative handoff artifact.
Prompts are scaffolding to point there.
* Parallel sessions (Prompts 2 + 3) can run safely because they touch
different cartridge directories with no shared code. The coord layer
guarantees their agents can signal each other without collision.
* Update this prompt file if the task set shifts — it’s a pointer to the
design log, which is the real source.
